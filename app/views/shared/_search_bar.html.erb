<div class="search--bar">
  <%= form_tag feed_users_path, method: :get do %>
    <%= text_field_tag :query,
      params[:query],
      class: "form--input",
      placeholder: "Search"
    %>
      <!-- image_submit_tag('search.png') -->
  <% end %>
</div>
